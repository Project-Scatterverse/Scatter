name: Scatter
version: '${version}'
main: dev.lirox.scatter.Scatter
api-version: '1.21'
prefix: "[Scatter]"
commands:
  scatter:
    description: "* apply scatter"
    usage: "/<command> while holding an item"
  reviver:
    description: "* apply reviver"
    usage: "/<command> while holding an item"
  state:
    description: "* "
    usage: "/<command> player regular/tagged/trapped/ghost/meowthpiece"